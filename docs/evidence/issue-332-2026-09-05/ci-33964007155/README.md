# Actual CI artifacts — run33964007155

Exact workflow-dispatch head64a90ef978ca9c3485a9b767c9076a0202ea2856 completed hosted CI and CodeQL successfully. Downloaded original artifacts remain in the session evidence directory; hashes and all127 packaged src/data file comparisons are recorded in source-byte-comparison.json. Both archives match the exact commit, pass the corrected payload verifier, install in separate environments and complete all14 recorded commands, including actual pinned Packaging get/info/API. No artifact is substituted by a local rebuild.

This is the archive-verifier head; the subsequent final CLI help change and preparation documentation refresh require their own final CI. It does not claim a published release.
