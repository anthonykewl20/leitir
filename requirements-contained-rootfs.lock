# Exact wheel bytes included in the immutable containment rootfs. Keep this
# wheel-only hash lock so the rootfs digest cannot silently change on rebuild.
idna==3.18 \
    --hash=sha256:7f952cbe720b688055e3f87de14f5c3e5fdaa8bc3928985c4077ca689de849a2
