{
  "case": "shadowing",
  "consumer_dir": "consumer",
  "description": "A local function definition rebinds the name the import introduced, so the binding is recorded as a re-export rather than a plain resolution.",
  "expect": "valid",
  "expected_state": "re-export",
  "kind": "shadowing",
  "report_file": "report.json",
  "requirements_file": "requirements.txt",
  "schema_version": "leitir-usage-fixture-manifest-v1"
}
