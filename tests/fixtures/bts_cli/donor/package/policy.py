# Contract-shaped synthetic donor for the BTS compute CLI tests.
# The comments deliberately keep the selected function below the 40% default
# donor-source ratio while retaining a tiny single-package repository.
# No dynamic constructs or imports are present.
def normalize_contract(value):
    return value
