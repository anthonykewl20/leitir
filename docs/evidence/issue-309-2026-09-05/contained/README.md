# Real contained Phase A evidence

Run: https://github.com/anthonykewl20/leitir/actions/runs/33953061013

All five pinned donors execute under real nsjail containment and complete their pipeline checks with exact rerun parity and donor-import rejection proof. The aggregate report remains REJECT pending independent owner ratification. The same runtime digest was observed in run 33951188380. `projection.json` is the unsigned canonical signing input, not a signature or authority. The trusted key remains unchanged.

The observation workflow used the documented unratified Phase A protocol on a separate validation branch. It did not change production gate behavior or publish a rootfs. The active corpus and sidecar remain unchanged until independent ratification is supplied and Phase C passes.
