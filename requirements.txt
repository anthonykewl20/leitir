# Fully pinned Python 3.11+ development and runtime dependency closure.
# Regenerate in a clean environment when changing pyproject.toml dependencies.
# pytest-xdist/execnet are CI-wall-time tooling only (-n auto); the canonical
# local run stays serial: PYTHONPATH=src uv run --no-project
# --with-requirements requirements.txt python -m pytest -q
build==1.3.0
execnet==2.1.2
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
Pygments==2.20.0
pyproject_hooks==1.2.0
pytest==9.0.3
pytest-xdist==3.8.0
setuptools==83.0.0
wheel==0.46.3
