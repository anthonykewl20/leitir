import { Base, greet } from "./base.js";
import "./base.js";

export class Child extends Base {
  async method() {
    greet();
    new Base();
    receiver.run();
    throw new Base();
    throw value;
  }
}
